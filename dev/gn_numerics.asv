% 3d gaussian
g1(t) = @(t) exp(-t.^2./(2.*l^2));
g2(t) = @(t) 1-abs(g1(t)).^2;
g3(t1,t2) = @(t1,t2) 1-g1(t1).^2-g1(t2).^2-g1(t1-t2).^2+2.*g1(t1).*g1(t2)*g1(t1-t2);

t_max = 3;

t1 = rand(1,num_pts).*t_max;
t2 = rand(1,num_pts).*t_max;

g1_data = [g1(t1);g1(t2)];
g2

% resolution

% saturation

% binning

